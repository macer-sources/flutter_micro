name: micro_flutter
description: "组件化解决方案"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.5.2


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_native_splash: ^2.4.1
  flutter_launcher_icons: ^0.13.1
  # 添加module 组件引用
  core:
    path: modules/basic/core
  network:
    path: modules/basic/network
  router_path:
    path: modules/business/router_path
  login:
    path: modules/business/login
  home:
    path: modules/business/home
  profile:
    path: modules/business/profile
  chat:
    path: modules/business/chat




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  melos: ^6.1.0

flutter:
  uses-material-design: true

flutter_icons:
  android: true           # 启用 Android 图标生成
  ios: true               # 启用 iOS 图标生成
  image_path: "assets/imgs/app_icon.png"  # 指定你的应用图标路径
  # 运行命令： flutter pub run flutter_launcher_icons:main




